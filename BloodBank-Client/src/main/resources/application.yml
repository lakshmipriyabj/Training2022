server:
  port: 7090
  
spring:
  application:
    name: BloodBank-service-client  
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka