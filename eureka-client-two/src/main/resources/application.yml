server:
  port: 6065
  
spring:
  application:
    name: client-Two  
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
